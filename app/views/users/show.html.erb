<h1>My Profile</h1>
<%= @user.name %>
<%= @user.title %>
<%= @user.location %>


<h2>My Rigs</h2>
<%= link_to "New Rig", new_user_rig_path(@user) %>
<% @user.rigs.each do |rig| %>
<%= link_to rig.name, rig_path(rig) %><br>
<% end %>